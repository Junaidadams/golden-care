---
import { siteName } from "../../constants"
import { Image } from "astro:assets";
import logo from "../assets/logo.png"
---
<a href="/" class="flex items-center gap-2 text-xl font-bold text-brand-600 font-fraunces">
		<Image src={logo} alt={`${siteName} logo`} width={100} height={33} />

			{siteName}
</a>